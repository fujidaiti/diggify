import 'package:diggify/design/sizes.dart';
import 'package:gap/gap.dart';

class Spacer {
  const Spacer._();

  static const tiny = Gap(tinySpaceSize);
  static const extraSmall = Gap(extraSmallSpaceSize);
  static const small = Gap(smallSpaceSize);
  static const medium = Gap(mediumSpaceSize);
  static const large = Gap(largeSpaceSize);
  static const extraLarge = Gap(extraLargeSpaceSize);
}
